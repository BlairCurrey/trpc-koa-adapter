{
  // Build config: source only, so rootDir keeps the emitted files at dist/index.js.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "declaration": true,
    "noEmit": false
  },
  "include": ["src/**/*"]
}
